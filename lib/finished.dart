import 'package:flutter/material.dart';

class FinishedGames extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Partidas finalizadas'),
          centerTitle: true,
          backgroundColor: Colors.green[900],
        ),
        body:Container(

        )
    );
  }
}