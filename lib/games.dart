import 'package:flutter/material.dart';

class CurrentGames extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Partidas actuales'),
          centerTitle: true,
          backgroundColor: Colors.green[900],
        ),
        body:Container(

        )
    );
  }
}